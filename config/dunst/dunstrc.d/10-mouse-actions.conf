# Drop-in override: make left-click run the notification's default action
# instead of just dismissing it (dunst's stock default requires middle-click).
[global]
    mouse_left_click = do_action, close_current
    mouse_middle_click = close_all
    mouse_right_click = close_current
